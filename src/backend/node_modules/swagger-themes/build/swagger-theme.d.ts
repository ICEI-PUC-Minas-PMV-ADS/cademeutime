import { DefaultConfig, SwaggerThemeName, VersionSwagger } from './types';
export declare class SwaggerTheme {
    readonly version: VersionSwagger;
    constructor(version: VersionSwagger);
    getBuffer(name: SwaggerThemeName): string;
    getDefaultConfig(theme: SwaggerThemeName): DefaultConfig;
}
//# sourceMappingURL=swagger-theme.d.ts.map