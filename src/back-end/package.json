{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "cli:run" : "nodemon --exec node --loader ts-node/esm src/server.ts",
    "dev": "set API_ENVIRONMENT=DEV && npm run cli:run",
    "qa": "set API_ENVIRONMENT=QA && npm run cli:run",    
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^4.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.6.0",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
